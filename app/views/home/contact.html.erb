<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contato - NaviDelivery</title>
  <meta name="description" content="Entre em contato conosco. Estamos aqui para ajudar você a transformar suas entregas.">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    /* Reutilizar estilos base */
    <%= File.read(Rails.root.join('app', 'views', 'home', 'index.html.erb')).match(/<style>(.*?)<\/style>/m)[1] %>

    .contact-page {
      padding: 8rem 0 4rem;
      min-height: 100vh;
    }

    .contact-hero {
      text-align: center;
      margin-bottom: 4rem;
    }

    .contact-hero h1 {
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: 1rem;
      background: var(--gradient-primary);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .contact-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: start;
    }

    .contact-form {
      background: white;
      padding: 3rem;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 500;
      color: var(--text-primary);
    }

    .form-group input,
    .form-group select,
    .form-group textarea {
      width: 100%;
      padding: 0.75rem;
      border: 2px solid var(--border);
      border-radius: 8px;
      font-size: 1rem;
      transition: border-color 0.3s ease;
    }

    .form-group input:focus,
    .form-group select:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: var(--primary-color);
    }

    .form-group textarea {
      resize: vertical;
      min-height: 120px;
    }

    .submit-button {
      width: 100%;
      background: var(--primary-color);
      color: white;
      padding: 1rem;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .submit-button:hover {
      background: var(--primary-dark);
      transform: translateY(-2px);
    }

    .contact-info {
      padding: 2rem 0;
    }

    .contact-method {
      background: var(--surface);
      padding: 2rem;
      border-radius: 12px;
      margin-bottom: 2rem;
      text-align: center;
    }

    .contact-icon {
      font-size: 2rem;
      margin-bottom: 1rem;
      display: block;
    }

    .contact-method h3 {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--text-primary);
    }

    .contact-method p {
      color: var(--text-secondary);
      margin-bottom: 1rem;
    }

    .contact-method a {
      color: var(--primary-color);
      text-decoration: none;
      font-weight: 500;
    }

    .office-hours {
      background: var(--gradient-primary);
      color: white;
      padding: 2rem;
      border-radius: 12px;
      text-align: center;
    }

    .office-hours h3 {
      margin-bottom: 1rem;
    }

    @media (max-width: 768px) {
      .contact-content {
        grid-template-columns: 1fr;
        gap: 2rem;
      }

      .contact-form {
        padding: 2rem;
      }
    }
  </style>
</head>

<body>
  <header class="header">
    <nav class="container">
      <div class="nav">
        <%= link_to "NaviDelivery", root_path, class: "logo" %>
        <%= link_to "← Voltar", root_path, class: "nav-links" %>
        <%= link_to "Teste Grátis", new_user_registration_path, class: "cta-button" %>
      </div>
    </nav>
  </header>

  <main class="contact-page">
    <div class="container">
      <div class="contact-hero">
        <h1>Vamos Conversar?</h1>
        <p class="section-subtitle">
          Estamos aqui para ajudar você a transformar suas entregas.
          Entre em contato e descubra como podemos impulsionar seu negócio.
        </p>
      </div>

      <div class="contact-content">
        <div class="contact-form">
          <h2 style="margin-bottom: 2rem; color: var(--text-primary);">Envie sua Mensagem</h2>

          <%= form_with url: contact_path, method: :post, local: true, id: "contact-form" do |form| %>
            <div class="form-group">
              <%= form.label :name, "Nome Completo *" %>
              <%= form.text_field :name, required: true, placeholder: "Seu nome completo" %>
            </div>

            <div class="form-group">
              <%= form.label :email, "Email *" %>
              <%= form.email_field :email, required: true, placeholder: "seu@email.com" %>
            </div>

            <div class="form-group">
              <%= form.label :phone, "Telefone" %>
              <%= form.tel_field :phone, placeholder: "(11) 99999-9999" %>
            </div>

            <div class="form-group">
              <%= form.label :company, "Empresa" %>
              <%= form.text_field :company, placeholder: "Nome da sua empresa" %>
            </div>

            <div class="form-group">
              <%= form.label :subject, "Assunto *" %>
              <%= form.select :subject, [
                ['Dúvidas sobre o produto', 'product'],
                ['Solicitar demonstração', 'demo'],
                ['Planos e preços', 'pricing'],
                ['Suporte técnico', 'support'],
                ['Parceria comercial', 'partnership'],
                ['Outro', 'other']
              ], { prompt: 'Selecione um assunto' }, { required: true } %>
            </div>

            <div class="form-group">
              <%= form.label :message, "Mensagem *" %>
              <%= form.text_area :message, required: true,
                  placeholder: "Conte-nos mais sobre sua necessidade. Quanto mais detalhes, melhor poderemos ajudar!" %>
            </div>

            <button type="submit" class="submit-button">
              Enviar Mensagem
            </button>
          <% end %>
        </div>

        <div class="contact-info">
          <div class="contact-method">
            <span class="contact-icon">📧</span>
            <h3>Email</h3>
            <p>Resposta em até 24 horas</p>
            <a href="mailto:contato@navidelivery.com">contato@navidelivery.com</a>
          </div>

          <div class="contact-method">
            <span class="contact-icon">📱</span>
            <h3>WhatsApp</h3>
            <p>Atendimento rápido e direto</p>
            <a href="https://wa.me/5511999999999" target="_blank">(11) 99999-9999</a>
          </div>

          <div class="contact-method">
            <span class="contact-icon">💬</span>
            <h3>Chat Online</h3>
            <p>Suporte em tempo real</p>
            <a href="#" onclick="openChat()">Iniciar conversa</a>
          </div>

          <div class="office-hours">
            <h3>Horário de Atendimento</h3>
            <p><strong>Segunda a Sexta:</strong> 9h às 18h</p>
            <p><strong>Sábado:</strong> 9h às 14h</p>
            <p><strong>Domingo:</strong> Apenas emergências</p>
            <p style="margin-top: 1rem; opacity: 0.9;">
              <small>Planos Enterprise têm suporte 24/7</small>
            </p>
          </div>
        </div>
      </div>
    </div>
  </main>

  <script>
    // Form submission handling
    document.getElementById('contact-form').addEventListener('submit', function(e) {
      e.preventDefault();

      const submitButton = this.querySelector('.submit-button');
      const originalText = submitButton.textContent;

      // Loading state
      submitButton.textContent = 'Enviando...';
      submitButton.disabled = true;
      submitButton.style.opacity = '0.7';

      // Simulate form submission (replace with actual implementation)
      setTimeout(() => {
        alert('Mensagem enviada com sucesso! Entraremos em contato em breve.');

        // Reset form
        this.reset();
        submitButton.textContent = originalText;
        submitButton.disabled = false;
        submitButton.style.opacity = '1';
      }, 2000);
    });

    // Phone number formatting
    document.querySelector('input[name="phone"]').addEventListener('input', function(e) {
      let value = e.target.value.replace(/\D/g, '');
      if (value.length >= 11) {
        value = value.replace(/(\d{2})(\d{5})(\d{4})/, '($1) $2-$3');
      } else if (value.length >= 7) {
        value = value.replace(/(\d{2})(\d{4})(\d{0,4})/, '($1) $2-$3');
      } else if (value.length >= 3) {
        value = value.replace(/(\d{2})(\d{0,5})/, '($1) $2');
      }
      e.target.value = value;
    });

    // Chat function
    function openChat() {
      // Implementar integração com sistema de chat
      alert('Chat será implementado em breve. Use nosso WhatsApp para atendimento imediato!');
    }

    // Form validation enhancement
    document.querySelectorAll('input[required], select[required], textarea[required]').forEach(field => {
      field.addEventListener('blur', function() {
        if (!this.value.trim()) {
          this.style.borderColor = '#ef4444';
        } else {
          this.style.borderColor = '#10b981';
        }
      });

      field.addEventListener('input', function() {
        if (this.style.borderColor === 'rgb(239, 68, 68)' && this.value.trim()) {
          this.style.borderColor = '#e5e7eb';
        }
      });
    });
  </script>
</body>
</html>
