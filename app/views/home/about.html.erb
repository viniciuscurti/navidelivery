<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sobre Nós - NaviDelivery</title>
  <meta name="description" content="Conheça a história, missão e valores da NaviDelivery. Somos especialistas em transformar operações de entrega.">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    /* Reutilizar estilos base */
    <%= File.read(Rails.root.join('app', 'views', 'home', 'index.html.erb')).match(/<style>(.*?)<\/style>/m)[1] %>

    .about-page {
      padding: 8rem 0 4rem;
    }

    .about-hero {
      text-align: center;
      margin-bottom: 6rem;
      background: var(--gradient-primary);
      color: white;
      padding: 4rem 0;
      border-radius: 16px;
    }

    .about-hero h1 {
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: 1rem;
    }

    .story-section {
      margin-bottom: 6rem;
    }

    .story-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
    }

    .story-text h2 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 2rem;
      color: var(--primary-color);
    }

    .story-text p {
      font-size: 1.1rem;
      line-height: 1.8;
      color: var(--text-secondary);
      margin-bottom: 1.5rem;
    }

    .story-image {
      background: var(--gradient-secondary);
      height: 400px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.2rem;
    }

    .values-section {
      background: var(--surface);
      padding: 4rem 0;
      border-radius: 16px;
      margin-bottom: 6rem;
    }

    .values-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 3rem;
      margin-top: 3rem;
    }

    .value-item {
      text-align: center;
      padding: 2rem;
    }

    .value-icon {
      font-size: 3rem;
      margin-bottom: 1.5rem;
      display: block;
    }

    .value-title {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1rem;
      color: var(--text-primary);
    }

    .value-description {
      color: var(--text-secondary);
      line-height: 1.6;
    }

    .team-section {
      margin-bottom: 6rem;
    }

    .team-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }

    .team-member {
      background: white;
      padding: 2rem;
      border-radius: 12px;
      text-align: center;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
      transition: transform 0.3s ease;
    }

    .team-member:hover {
      transform: translateY(-8px);
    }

    .member-photo {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      margin: 0 auto 1.5rem;
      background: var(--gradient-primary);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 2rem;
    }

    .member-name {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--text-primary);
    }

    .member-role {
      color: var(--primary-color);
      font-weight: 500;
      margin-bottom: 1rem;
    }

    .member-bio {
      color: var(--text-secondary);
      font-size: 0.9rem;
      line-height: 1.5;
    }

    .stats-section {
      background: var(--gradient-primary);
      color: white;
      padding: 4rem 0;
      border-radius: 16px;
      text-align: center;
      margin-bottom: 6rem;
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }

    .stat-item {
      padding: 1.5rem;
    }

    .stat-number {
      font-size: 3rem;
      font-weight: 800;
      display: block;
      margin-bottom: 0.5rem;
    }

    .stat-label {
      font-size: 1.1rem;
      opacity: 0.9;
    }

    .mission-section {
      text-align: center;
      margin-bottom: 4rem;
    }

    .mission-content {
      max-width: 800px;
      margin: 0 auto;
      background: white;
      padding: 4rem;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    }

    .mission-content h2 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 2rem;
      color: var(--primary-color);
    }

    .mission-content p {
      font-size: 1.2rem;
      line-height: 1.8;
      color: var(--text-secondary);
    }

    @media (max-width: 768px) {
      .about-hero h1 {
        font-size: 2rem;
      }

      .story-content {
        grid-template-columns: 1fr;
        text-align: center;
      }

      .story-text h2 {
        font-size: 2rem;
      }

      .values-grid {
        grid-template-columns: 1fr;
      }

      .team-grid {
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      }

      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .mission-content {
        padding: 2rem;
      }
    }
  </style>
</head>

<body>
  <header class="header">
    <nav class="container">
      <div class="nav">
        <%= link_to "NaviDelivery", root_path, class: "logo" %>
        <%= link_to "← Voltar", root_path, class: "nav-links" %>
        <%= link_to "Teste Grátis", new_user_registration_path, class: "cta-button" %>
      </div>
    </nav>
  </header>

  <main class="about-page">
    <div class="container">
      <div class="about-hero">
        <h1>Transformando o Futuro das Entregas</h1>
        <p class="section-subtitle">
          Somos especialistas em tecnologia de entrega, ajudando empresas a
          criar experiências excepcionais para seus clientes.
        </p>
      </div>

      <section class="story-section">
        <div class="story-content">
          <div class="story-text">
            <h2>Nossa História</h2>
            <p>
              Fundada em 2023 por uma equipe experiente em logística e tecnologia,
              a NaviDelivery nasceu da necessidade de simplificar e otimizar
              operações de entrega para empresas de todos os tamanhos.
            </p>
            <p>
              Percebemos que muitas empresas enfrentavam os mesmos desafios:
              falta de visibilidade das entregas, comunicação ineficiente com clientes,
              e dificuldade para escalar operações. Foi assim que decidimos criar
              uma plataforma completa e acessível.
            </p>
            <p>
              Hoje, orgulhosamente servimos centenas de empresas em todo o Brasil,
              processando milhares de entregas diariamente e sempre focados em
              inovação e excelência no atendimento.
            </p>
          </div>
          <div class="story-image">
            <span>📈 Crescimento Constante</span>
          </div>
        </div>
      </section>

      <section class="values-section">
        <div class="section-header">
          <h2 class="section-title">Nossos Valores</h2>
          <p class="section-subtitle">
            Os princípios que guiam cada decisão e produto que desenvolvemos
          </p>
        </div>

        <div class="values-grid">
          <div class="value-item">
            <span class="value-icon">🎯</span>
            <h3 class="value-title">Foco no Cliente</h3>
            <p class="value-description">
              Cada funcionalidade é pensada para resolver problemas reais
              e gerar valor tangível para nossos clientes.
            </p>
          </div>

          <div class="value-item">
            <span class="value-icon">⚡</span>
            <h3 class="value-title">Inovação Constante</h3>
            <p class="value-description">
              Investimos continuamente em novas tecnologias para manter
              nossa plataforma sempre à frente do mercado.
            </p>
          </div>

          <div class="value-item">
            <span class="value-icon">🤝</span>
            <h3 class="value-title">Transparência</h3>
            <p class="value-description">
              Comunicação clara, preços justos e processos transparentes
              em todos os aspectos do nosso relacionamento.
            </p>
          </div>

          <div class="value-item">
            <span class="value-icon">🔒</span>
            <h3 class="value-title">Segurança</h3>
            <p class="value-description">
              Proteção de dados e operações seguras são prioridades
              absolutas em tudo que desenvolvemos.
            </p>
          </div>

          <div class="value-item">
            <span class="value-icon">🚀</span>
            <h3 class="value-title">Excelência</h3>
            <p class="value-description">
              Buscamos a perfeição em cada detalhe, desde o código
              até o atendimento ao cliente.
            </p>
          </div>

          <div class="value-item">
            <span class="value-icon">🌱</span>
            <h3 class="value-title">Sustentabilidade</h3>
            <p class="value-description">
              Desenvolvemos soluções que otimizam rotas e reduzem
              impacto ambiental das operações de entrega.
            </p>
          </div>
        </div>
      </section>

      <section class="stats-section">
        <h2 style="margin-bottom: 1rem;">NaviDelivery em Números</h2>
        <p style="opacity: 0.9; margin-bottom: 2rem;">
          Resultados que comprovam nossa expertise e comprometimento
        </p>

        <div class="stats-grid">
          <div class="stat-item">
            <span class="stat-number">500K+</span>
            <span class="stat-label">Entregas Processadas</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">200+</span>
            <span class="stat-label">Empresas Atendidas</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">99.9%</span>
            <span class="stat-label">Uptime da Plataforma</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">4.9/5</span>
            <span class="stat-label">Satisfação do Cliente</span>
          </div>
        </div>
      </section>

      <section class="team-section">
        <div class="section-header">
          <h2 class="section-title">Nossa Equipe</h2>
          <p class="section-subtitle">
            Profissionais experientes e apaixonados por tecnologia e logística
          </p>
        </div>

        <div class="team-grid">
          <div class="team-member">
            <div class="member-photo">👨‍💼</div>
            <h3 class="member-name">Carlos Silva</h3>
            <p class="member-role">CEO & Co-fundador</p>
            <p class="member-bio">
              15 anos de experiência em logística e operações.
              Ex-diretor de uma das maiores transportadoras do país.
            </p>
          </div>

          <div class="team-member">
            <div class="member-photo">👩‍💻</div>
            <h3 class="member-name">Ana Santos</h3>
            <p class="member-role">CTO & Co-fundadora</p>
            <p class="member-bio">
              Especialista em arquitetura de software escalável.
              10 anos desenvolvendo soluções para startups e grandes empresas.
            </p>
          </div>

          <div class="team-member">
            <div class="member-photo">👨‍🔬</div>
            <h3 class="member-name">Pedro Oliveira</h3>
            <p class="member-role">Head de Produto</p>
            <p class="member-bio">
              Product Manager experiente com foco em UX/UI.
              Especialista em otimização de processos e dados.
            </p>
          </div>

          <div class="team-member">
            <div class="member-photo">👩‍💼</div>
            <h3 class="member-name">Maria Costa</h3>
            <p class="member-role">Head de Customer Success</p>
            <p class="member-bio">
              Especialista em atendimento ao cliente e operações.
              Focada em garantir o sucesso de cada parceiro.
            </p>
          </div>
        </div>
      </section>

      <section class="mission-section">
        <div class="mission-content">
          <h2>Nossa Missão</h2>
          <p>
            <strong>Democratizar tecnologia de ponta para operações de entrega,</strong>
            tornando-a acessível para empresas de todos os tamanhos. Acreditamos que
            toda empresa, independente do porte, deve ter acesso às melhores ferramentas
            para oferecer experiências excepcionais aos seus clientes.
          </p>
          <br>
          <p>
            Nosso compromisso é continuar inovando e crescendo junto com nossos clientes,
            sempre mantendo a simplicidade, eficiência e excelência no centro de tudo que fazemos.
          </p>
        </div>
      </section>
    </div>
  </main>

  <script>
    // Animate stats on scroll
    const observerOptions = {
      threshold: 0.5,
      rootMargin: '0px'
    };

    const statsObserver = new IntersectionObserver(function(entries) {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const stats = entry.target.querySelectorAll('.stat-number');
          stats.forEach(stat => {
            animateNumber(stat);
          });
        }
      });
    }, observerOptions);

    const statsSection = document.querySelector('.stats-section');
    if (statsSection) {
      statsObserver.observe(statsSection);
    }

    function animateNumber(element) {
      const text = element.textContent;
      const number = parseInt(text.replace(/\D/g, ''));
      const suffix = text.replace(/[\d,\.]/g, '');

      if (isNaN(number)) return;

      let current = 0;
      const increment = number / 50;
      const timer = setInterval(() => {
        current += increment;
        if (current >= number) {
          current = number;
          clearInterval(timer);
        }

        if (suffix.includes('K')) {
          element.textContent = Math.floor(current / 1000) + 'K' + suffix.replace('K', '');
        } else if (suffix.includes('.')) {
          element.textContent = (current / 10).toFixed(1) + suffix;
        } else {
          element.textContent = Math.floor(current) + suffix;
        }
      }, 50);
    }

    // Team member hover effects
    document.querySelectorAll('.team-member').forEach(member => {
      member.addEventListener('mouseenter', function() {
        this.style.boxShadow = '0 20px 40px rgba(59, 130, 246, 0.2)';
      });

      member.addEventListener('mouseleave', function() {
        this.style.boxShadow = '0 4px 6px rgba(0, 0, 0, 0.05)';
      });
    });
  </script>
</body>
</html>

