<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom sticky-top">
  <div class="container">
    <%= link_to 'NaviDelivery', root_path, class: 'navbar-brand fw-semibold' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Alternar navegação">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link" href="#features">Funcionalidades</a></li>
        <li class="nav-item"><a class="nav-link" href="#metrics">Métricas</a></li>
        <li class="nav-item"><a class="nav-link" href="#pricing">Preços</a></li>
        <li class="nav-item"><a class="nav-link" href="#faq">FAQ</a></li>
      </ul>
      <div class="d-flex gap-2">
        <% if user_signed_in? %>
          <%= link_to 'Painel', admin_path, class: 'btn btn-sm btn-primary' %>
          <%= link_to 'Sair', destroy_user_session_path, method: :delete, class: 'btn btn-sm btn-outline-secondary' %>
        <% else %>
          <%= link_to 'Entrar com Google', (defined?(user_google_oauth2_omniauth_authorize_path) ? user_google_oauth2_omniauth_authorize_path : new_user_session_path), class: 'btn btn-sm btn-primary' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
